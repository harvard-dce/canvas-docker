canvas:
    build: .
    command: /bin/sh
    ports:
        - "3000:80"
    expose:
        - "80"
    links:
        - redis
        - pg

redis:
    image: redis
    expose:
        - "6379"

pg:
    build: ./postgres
    expose:
        - "5432"
